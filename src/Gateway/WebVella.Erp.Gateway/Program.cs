// Minimal Gateway Program.cs entry point — stub for compilation.
// Full implementation will be created by the assigned agent.
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();
app.Run();
