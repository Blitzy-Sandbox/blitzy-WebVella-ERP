{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "crm",
  "root": "services/crm",
  "sourceRoot": "services/crm/src",
  "projectType": "application",
  "tags": ["type:service", "scope:crm", "runtime:dotnet"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "dotnet publish ../Crm.csproj -c Release -o dist/",
        "cwd": "services/crm/src"
      },
      "dependsOn": ["^build"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet test ../tests/ --configuration Release --logger trx",
        "cwd": "services/crm/src"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet format ../Crm.csproj --verify-no-changes",
        "cwd": "services/crm/src"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet run --project ../Crm.csproj",
        "cwd": "services/crm/src"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cdklocal deploy CrmStack --context localstack=true --require-approval never",
        "cwd": "infra"
      },
      "dependsOn": ["build"]
    }
  }
}
