{
	// SECURITY (A02/A05 hardcoded secrets — CWE-798): committed secrets removed from source. Supply at runtime via
	// environment variables (":" nests as "__", e.g. Settings__Jwt__Key) or user-secrets. Never commit real values.
	"Settings": {
		// SECURITY (CWE-798): DB credentials removed. Provide the FULL connection string via env var Settings__ConnectionString,
		// including pooling/timeout to preserve the performance envelope, e.g.
		// "Server=<host>;Port=<port>;User Id=<user>;Password=<pwd>;Database=<db>;Pooling=true;MinPoolSize=1;MaxPoolSize=100;CommandTimeout=120;"
		"ConnectionString": "",
		// SECURITY (CWE-798): encryption key removed. Provide via env var Settings__EncryptionKey (64 hex chars).
		"EncryptionKey": "",
		"Lang": "en",
		"Locale": "en-US",
		"TimeZoneName": "FLE Standard Time",
		// SECURITY (A05 / CWE-215): production must not disclose stack traces / dev diagnostics — DevelopmentMode disabled.
		"DevelopmentMode": "false",
		"EnableBackgroungJobs": "true",
		"EnableFileSystemStorage": "false",
		"FileSystemStorageFolder": "\\\\192.168.10.25\\Share\\erp3-files",
		"EmailEnabled": false,
		"EmailSMTPServerName": "",
		"EmailSMTPPort": "25",
		"EmailSMTPUsername": "",
		"EmailSMTPPassword": "",
		"EmailFrom": "",
		"EmailTo": "",
		"Jwt": {
			// SECURITY (CWE-798): default JWT signing key removed. Provide a strong key via env var Settings__Jwt__Key
			// (>= 32 bytes / 256-bit for HMAC-SHA-256). Rotating this key invalidates all in-flight tokens.
			"Key": "",
			"Issuer": "webvella-erp",
			"Audience": "webvella-erp"
		}
	},
	"Development": {
		"TestEntityName": "test",
		"TestRecordId": "001ea36f-fd2e-4d1b-b8ee-25d32d4e396c"
	},
	"ApiUrlTemplates": {
		"FieldInlineEdit": "/api/v3/en_US/record/{entityName}/{recordId}"
	}

}