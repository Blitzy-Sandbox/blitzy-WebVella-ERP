<Project>

  <!--
    Solution-wide build properties (OWASP A06 / SCA).

    AutoMapper advisory GHSA-rvv3-g6hj-g44x / CVE-2026-32933 (CWE-674, uncontrolled recursion /
    denial of service) is now resolved at the dependency level: AutoMapper is upgraded from
    14.0.0 to the patched release 16.1.1 (see WebVella.Erp/WebVella.Erp.csproj). Because the
    package is patched, both the NU1903 build advisory and the dotnet vulnerable-package SCA scan
    are clear, so no NuGetAuditSuppress entry is required (and none is kept here; a suppression
    would only mask, not fix, the finding). See SECURITY.md (F-11) for the full resolution record
    and the AutoMapper licensing note.
  -->

</Project>
